# AndroidX is required by AGP 8; jetifier stays off (no legacy support libs).
android.useAndroidX=true
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
# The engine binary is copied in by fetch-engine.sh, not built by gradle.
